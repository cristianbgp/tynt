# platformer

Jump through a compact level and collect every coin.

**Controls:** Left/right move · A jumps · B resets

This cartridge combines gravity with `overlap()` checks, collectible state, `every()` animation, and synthesized win feedback.

This cartridge is maintained in the tynt public gallery.
