# snake

Eat procedurally placed food, grow, and avoid the walls and yourself.

**Controls:** Arrows steer · A or B restarts

This cartridge uses `seed()` and `random()` for repeatable food placement, `every()` for grid timing, `pointInRect()` for the arena boundary, and synthesized sound for clear feedback.

This cartridge is maintained in the tynt public gallery.
