# flappy

Fly through seeded, changing pipe gaps and chase a higher score.

**Controls:** A flaps · B resets · A or B retries

This cartridge uses deterministic `random()` pipe gaps, `overlap()` collision checks, gravity, recycling obstacles, and small sound cues.

This cartridge is maintained in the tynt public gallery.
