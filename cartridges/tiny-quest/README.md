# tiny quest

Explore the scrolling maze, collect all three gems, then enter the outlined exit.

**Controls:** Arrows explore · B resets · A or B retries

This cartridge demonstrates two-axis `camera()` movement, deterministic placement, rectangle collisions with `overlap()`, goal zones with `pointInRect()`, and synthesized feedback.

This cartridge is maintained in the tynt public gallery.
