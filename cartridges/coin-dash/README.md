# coin dash

Collect five coins in a scrolling world before the 30-second timer expires.

**Controls:** Left/right move · A or B restarts after a round

This cartridge demonstrates a focused combination of `camera()`, deterministic `random()`, `overlap()`, frame timers, sprites, maps, and synthesized audio.

This cartridge is maintained in the tynt public gallery.
